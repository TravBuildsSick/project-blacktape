[package]
name = "bt-gui"
version = "0.1.0"
edition = "2021"

[dependencies]
slint = "1.8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rfd = "0.15"
tao = "0.35.3"
wry = "0.55.1"

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[build-dependencies]
slint-build = "1.8"
